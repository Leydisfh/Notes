{
  "name": "nodeejercicios",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build:ui": "cd ../notes-frontend && npm run build && cp -r dist ../notes-api-backend/public",
    "deploy:full": "npm run build:ui && cd ../notes-api-backend && git add . && git commit -m uibuild && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "2.8.5",
    "express": "4.19.2"
  }
}
